# Renomeie para secrets.toml (sem .example) e edite a senha.
# Nunca commite secrets.toml — está no .gitignore.
# No Streamlit Community Cloud, cole esse mesmo conteúdo em
# Settings → Secrets do app (interface web).

password = "trocar_essa_senha"
